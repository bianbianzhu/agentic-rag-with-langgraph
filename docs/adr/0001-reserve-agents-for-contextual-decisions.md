# Reserve agents for contextual decisions

The course uses agents only for bounded, non-deterministic decisions such as tool selection or workflow routing. Embedding, indexing, retrieval execution, and reranking remain deterministic components so their behavior is testable, observable, and independently replaceable without presenting ordinary pipeline stages as autonomous actors.
